# 交接文档 — 2026-06-22 小九 → 阿狸咪（终版）

> **最重要的事**：今天做了架构级变更，以后双机不会再冲突了。

---

## 🚨 阿狸咪回家后第一件事（唯一一步）

```bash
cd E:/workspace/stock-scanner
git pull origin main
```

> Git 仓库里不再有数据文件了。数据走坚果云自动同步。

---

## 今天做了什么（按时间顺序）

### 1. 🔧 代码数据分离（最重大变更）
- `.gitignore` 排除 `data/`、`dist/data/`、`dist/*.html`
- `batch_update.py` 双机同步简化为只 `git pull`，不再 commit/push 数据
- 67 个数据文件从 Git 中移除

### 2. 🎨 UI 调整
| 改动 | 说明 |
|------|------|
| 四喜临门 | 浅金色底色（`#fff8e1`），无数据时完全隐藏 |
| 中长线确认列表 | 序号去掉蓝色圆形徽章，改为纯数字 |
| 缠论视角 + 窗口失效 | 文字从 11px → 13px |
| 重点关注卡片 | 改为一眼结论同款丁香紫样式（白色底+紫色渐变头部） |

### 3. 🐛 Bug 修复
| 修复 | 说明 |
|------|------|
| 三线共振历史 | 端午节 6/19 不再误生成数据；脚本新增假日检测 |
| 日历审核 | 删除重复"港股休市"；FOMC 日期统一为北京时间决议公布日 |
| 日历数据丢失 | nt_data.json 被 stash 误删，已重新生成 |

---

## 新工作流

```
代码改动：git commit → git push → 对方 git pull
数据更新：fetch 脚本 → data/ → 坚果云自动同步 → 对方自动收到
部署：update_data_v2.py → deploy_now.py
```

**从此代码和数据永不交叉，双机永不冲突。**

---

## Git 最新

```
最新 commit: 65c038b+ (含 UI 调整、日历修复等多次提交)
首次分离:   7471f50 (代码数据分离)
```

---

_坚果云绿色勾 = 一切正常。有问题群里喊~_
