{
  "code": "# 坚果云防覆盖操作指南\n---\n\n## 问题\n坚果云同步 .git/ 目录导致两台电脑 Git 状态互相覆盖，产生 58 个冲突文件。\n\n## 根因\n坚果云把 `stock-scanner/` 当成普通文件夹同步，包括 `.git/` 内部文件。家里电脑修改代码后，通过坚果云覆盖到本机。\n\n## 解决方案（三保险）\n\n### 保险1：.nutstoreignore（阻断坚果云同步 Git 内部文件）\n\n在项目根目录 `/e/workspace/stock-scanner/` 创建 `.nutstoreignore`：\n\n```\n# 禁止同步 Git 内部文件\n.git/\n\n# 禁止同步构建产出\ndist/\n\n# 禁止同步缓存\n__pycache__/\n*.pyc\n*.bak\n\n# 禁止同步锁文件\n*.lock\n.deploy_lock\n.scanner.lock\n```\n\n**两台电脑都需要有这个文件**，坚果云客户端会识别并排除这些目录。\n\n### 保险2：sync_check.py 第5步（MD5 锁定检查）\n\n部署前自动检查 `index_master.html` 的 MD5 是否等于满意版。\n\n- 不匹配 → 自动 `git checkout HEAD -- index_master.html` 回退\n- 手动锁定：`python sync_check.py --lock`\n\n### 保险3：冲突清理\n\n`sync_check.py` 第1步自动清理所有 `*冲突*` 文件。\n\n## 分工原则\n\n| 通道 | 同步内容 | 工具 |\n|------|----------|------|\n| **GitHub** | 代码文件（*.py, *.html, *.js） | git push/pull |\n| **坚果云** | 数据文件（data/*.json）和交接文档 | 坚果云同步 |\n\n**记住**：代码走 GitHub，数据走坚果云，各管各的！\n\n## 操作步骤（小九执行）\n\n1. 确认 `stock-scanner/.nutstoreignore` 存在且内容正确（已完成）\n2. 在坚果云客户端里检查 `stock-scanner/` 同步设置，排除 `.git/` 和 `dist/`\n3. 运行 `python sync_check.py --lock` 锁定当前满意版\n\n## 操作步骤（阿狸咪执行）\n\n1. `git pull origin main` 获取最新代码（含 `.nutstoreignore` 和 `sync_check.py` 更新）\n2. 在坚果云客户端里检查 `stock-scanner/` 同步设置，排除 `.git/` 和 `dist/`\n3. 运行 `python sync_check.py --lock` 锁定当前满意版\n\n## 历史教训\n\n**2026-07-02**：坚果云覆盖导致满意版被旧版覆盖，58 个冲突文件。根因是之前仅在截图里商量了方案，没有落实到代码提交。\n- 已修复：提交 `25238d2` 建立三保险\n- 已清理：58 个冲突残留文件\n\n---\n\n**更新时间**：2026-07-02\n**锁定版本**：`index_master.html` MD5 = `37fbe6ff63cecbcf6a4972a7a442e783`\n", "language": "markdown"}
