# 📋 修改交接文档（周五19:00 → 周一07:10）

> **阿狸咪** 周六全天 + **workbuddy** 周日~周一早上
> 所有修改均已部署到 https://ah-quant999.github.io/quant-scanner-v6/
> 源码已推送 main 分支

---

## 一、🧬 多维共振系统（全新上线）🔴

### 1.1 五维评分公式 + TOP10 精选
- `generate_top10.py` — 五维加权评分（基础分+增强因子+资金+板块+机构）
- 金股观测页新增「🏆 每日TOP10精选」卡片 + 「🧬 多维共振」列
- 预判信号页新增 TOP3 共振王展示
- **个股查询页**：查询个股时显示多维评分标签（88分/#2/CIT…）

### 1.2 历史追踪系统
- `update_multi_resonance_daily.py` → `multi_resonance_history.json` 每日追加
- 独立页面 `multi_resonance.html`（日历视图+时间线）
- 入口：金股观测页「📊 历史追踪 →」按钮

### 1.3 止损/目标价
- generate_top10.py 输出 `stop_loss` + `target_price`
- 前端显示：🛑止损价（绿）+ 🎯目标价（红）

### 1.4 多维共振卡片精简（周一 07:07）
- 个股查询页：删除板块重复显示，评分明细从第二行合并到第一行
- 效果：`88 #2 C I T 基75增+9资0板0机4` 全部一行

---

## 二、🏗️ P0 行业映射系统（全新上线）🔴

- `fetch_industry_map.py`：东财80行业+54概念 → 反向映射
- `industry_map.json`：3908只股票 × 57板块，7121条关联
- 金股观测定时：每只股票后面显示板块标签，🔴流入 / 🟢流出着色
- batch_update close 自动执行（300s），周更新

---

## 三、🔥 涨停热力矩阵 + 联动分析（全新上线）🔴

- `fetch_limit_up_heatmap.py`：涨停→概念归类→10日热力矩阵
- 预判信号页「涨停联动分析」卡片 + 热力联动表格
- batch_update close 执行（120s）

---

## 四、🔴 板块资金 MOCK 数据修复（重大BUG）🟢

- **问题**：`fetch_sector_fund_flow.py` 一直用MOCK假数据（akshare升级后静默降级）
- **修复**：改用新API `stock_fund_flow_industry()` + `stock_fund_flow_concept()`
- 移除所有 MOCK 降级逻辑，失败返回空
- **铁律**：代码注释写入「宁可空着也不用假数据」
- 全站55个JSON文件审计，0个mock ✅

---

## 五、🔴 虚假"北向覆盖"加分清除（重大BUG）🟢

- **问题**：北向数据2024.5起已停更，但TOP10每只+2分加"北向覆盖"标签
- **修复**：generate_top10.py移除北向加分，TOP10全部-2分（数据真实了）
- **铁律贯彻**：宁可空着也不用假数据

---

## 六、🔴 密码注入失败修复（线上宕机级）🟢

- **问题**：用户报告 `cat999`/`hjd666` 密码均失效
- **根因**：`update_data_v2.py` 密码注入写入后未回读验证，Windows Defender锁文件致静默失败
- **修复**：写入后回读检查占位符是否真的被替换，失败重试3次+0.5s间隔

---

## 七、📦 暂不上架Tab（全新创建）

- 删除反复出问题旧隐藏数据页，新建 admin-only Tab
- 5张调试卡片：五维评分卡 / F窗口缠论 / 板块强度 / 主力抱团 / IPO评分
- 纯前端渲染，不碰 update_data_v2.py 管线

---

## 八、🔬 验证系统升级（v3→v4→v5→v6）

- **v3**：4→7项（+北向完整性/top10审计/评分源码审计）
- **v4**：41个核心JSON文件实质内容审计
- **v5**：27项akshare同源API交叉验证
- **v6**：fetch日志系统审计（连续失败≥3次→FAIL，>72h未运行→WARN）
- 所有13个自动化自动使用最新版本

---

## 九、📊 fetch日志系统（全新）🟢

- `fetch_logger.py` — 通用日志模块
- 全部33个fetch脚本集成 try/except + 日志记录
- `data/.fetch_log.json` — 连续失败数+最后错误+最后成功时间

---

## 十、🎨 UI/UX 修改（周六~周一）

### 10.1 一眼结论 → 行式布局 + 方向建议
- 每个指标独占一行（列表式）
- 末尾新增「🎯 方向建议」行（仓位判断+关注方向+回避方向+关键点位）
- 五维底色天青 ↔ 建议淡紫 互换

### 10.2 各Tab智能分析层
- 预判信号「一眼结论」→ 方向建议
- 概念涨跌榜 → 顶部方向判断条
- 全球速报 → 隔夜关键信号总结框
- 宏观观测 → 宏观综合研判（PMI/利差/VIX/国债四维评分）

### 10.3 板块资金轮动
- 板块资金流向 + 资金流向追踪 → 合并为一个卡片
- 新增 `generateSectorRotationAnalysis()` 自动生成轮动分析
- 三条线索合并为一行彩色badge
- 5日/20日/60日卡片只显示流入TOP5
- 副标题 `短线·盘中` → `中长线·盘后`

### 10.4 预判信号页瘦身（9卡→6卡）
- 隐藏五维评分卡/板块强度追踪/主力抱团监控（内容已被一眼结论覆盖）
- 卡片顺序：一眼结论→精选信号→机游共振→涨停联动→板块轮动→南向

### 10.5 F窗口+缠论 → 健康看板
- F窗口从数据监控页移到健康看板最顶端
- 三卡并排：F窗口(紫) | 缠论视角(橙) | 窗口失效应对(青)
- F窗口三行分组：已触发 | 当前活跃 | 即将到来
- "无" → "暂无触发"/"暂无活跃"/"暂无待观"
- 窗口失效应对新增点位区间（3734~3903点）

### 10.6 市场健康度三灯
- AI速览新增三灯：结构(连跌+累计) | 资金(主力净流) | 情绪(涨家+涨停)
- 绿灯≥2→偏暖 / 1灯→谨慎 / 0灯→观望
- 三灯下方虚线分隔 F窗口胶囊条

### 10.7 危机监测修复
- 趋势行语义修复：PMI/出口(正向指标，↑=改善)，SHIBOR/VIX(负向，↑=恶化)
- 新增 `up_is_good:true` 标记机制

### 10.8 打新研判 + 异动停牌
- 移入总览页（概念涨跌幅上方），风格统一为总览格式
- 无新股时打新卡片缩小为单行"📭 近期無新股上市"

### 10.9 涨停联动分析统一格式
- 边框 #ffcc80→#e65100（橙色竖线，匹配涨停主题）

### 10.10 五维评分卡恢复
- 从预判信号页隐藏状态 → 恢复到健康看板最顶端
- 白底+蓝紫竖线(#5c6bc0)，5列grid

### 10.11 个股查询页修改（周日~周一）
- 标签顺序：代码→名称→(主板)→板块概念标签
- 回调状态标题 `斐波那契窗口` 在前
- K线形态标签加"当日/多日"前缀
- K线形态合并板块异动→板块主线
- K线形态删除"更新于"时间戳，标签合并到标题同行（周一）
- 查询功能卡片重排：研报-持仓一行 / EMA-近10日一行 / 异动-停牌一行（周一）
- 多维共振卡片精简（周一）

### 10.12 金股观测页改版
- 三栏布局：多维共振 | 短线信号 | 中长线确认
- 多维共振筛选按钮提前（"全部"之后）

### 10.13 健康看板回调状态
- 判断顺序修复：深度回调→持续阴跌→阴跌磨底→强势上涨→震荡上行
- 状态徽章字体 12→14px

### 10.14 UI细节调整
- 机游共振左侧竖线 #004d40→#a5d6a7
- 精选预判信号更新时间 #283593→#aaa
- accent-bar 竖线 7px→3px
- 板块资金轮动真流出/真流入颜色反转（流出=绿，流入=红）
- F窗口胶囊条 10→12px
- 五维/建议底色互换
- 核心判断 5行→1行，详情删除

---

## 十一、📝 逻辑详解页更新（周六 23:50）

新增13个章节：
1. 五维评分卡评分表
2. 市场健康度三灯亮灯条件
3. F窗口+缠论+窗口失效应对
4. 板块资金轮动四条线索
5. 涨停联动热力矩阵
6. 多维共振评分完整公式
7. 行业映射构建逻辑
8. 涨停热力采集流程
9. 一眼结论方向建议
10. 宏观综合研判四维评分
11. 危机监测趋势语义修复
12. 机游共振判定

---

## 十二、📐 脚本新增/修改汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `generate_top10.py` | **新增** | 五维共振评分+TOP10生成 |
| `update_multi_resonance_daily.py` | **新增** | 历史追踪数据追加 |
| `multi_resonance.html` | **新增** | 历史追踪独立页面 |
| `fetch_industry_map.py` | **新增** | P0行业映射系统 |
| `fetch_limit_up_heatmap.py` | **新增** | 涨停热力矩阵采集 |
| `fetch_logger.py` | **新增** | 通用日志模块 |
| `verify_data_vs_website.py` | **升级** | v3→v6 |
| `fetch_sector_fund_flow.py` | **修复** | 移除MOCK，换新API |
| `fetch_52w_high.py` | **新增** | 52周新高采集 |
| `fetch_analyst_ratings.py` | **新增** | 分析师评级采集 |
| `update_data_v2.py` | **修改** | 密码注入回读+新markers+隐藏数据重写 |
| `index_master.html` | **大量修改** | 所有前端改动 |

---

## ⚠️ 已知遗留问题（低优先级）

1. `policy_density.json` 无fetch脚本，数据静态
2. `INDUSTRY_MAP` 182KB已注入但前端JS未完全使用（预留）
3. `sector_rs.json` strong_5d 为空 / `mahoro_signals.json` matches 空 / `ipo_data.json` stocks 空
4. `data/.deploy_lock` 偶尔残留，需手动清理
