# 交接文档 — 2026-06-22 晚间 阿狸咪 → 小九

> 今晚做了大量 UI 统一优化 + 部署管线加固，以下是小九接班需要注意的事项。

---

## 🎨 一、UI 版式调整（index_master.html）

### 字体统一规则（全站）
| 类型 | 字号 |
|------|------|
| 卡片标题 | 16px |
| 内容正文 | 13px |
| 更新于 | 12px |
| 标签 pill | 12px |

### 具体改动清单
| 卡片 | 改动 |
|------|------|
| AI市场速览 | 标题 14→16px，更新于 11→12px |
| 一眼结论 | 标题 14→16px，更新于 11→12px |
| 异常数据提醒 | 标题 14→16px，更新于 11→12px |
| 国家队ETF | 更新于 10→12px |
| 主力板块 | 更新于 10→12px |
| 核心判断 | 标题 12→14px，正文 11→13px |

### 中长线确认 — 删除了4处括号弧线
- 说明栏 `（机构变红+上涨趋势）` → 删除
- 分类标题 `（趋势跟随）` → 删除
- 展示范围 `（机构变红+上涨趋势，侧重趋势跟随）` → 删除
- 金股列标题 `（机构变红+上涨趋势）` → 删除

### 日历颜色 — typeColorMap 与图例已完全同步（20组）
- `earnings_japan`: `#e8eaf6/#4527a0`
- `earnings_europe`: `#fff8e1/#6d4c41`
- `kr_earnings` / `earnings_korea`: `#e0f2f1/#00695c`
- 其余 17 组已在之前同步完成

---

## 🛡️ 二、永久防冲突措施

### 1. deploy_now.py 自动同步源码（新功能！）

**每次部署完自动 commit + push 源码到 main**，从此告别"改完 UI 忘记 push 被覆盖"。

```python
# deploy_now.py 第5步（gh-pages部署成功后自动执行）
def _auto_push_source():
    git add -A     # .gitignore 自动过滤 data/ dist/
    git commit     # auto: source sync 06-23 00:11 — index_master.html
    git push origin main
```

> 小九每次 batch_update.py 启动时 `git pull` → 自动拉到阿狸咪的最新代码

### 2. .nojekyll 三层保护

```python
# 层1：清理旧文件时与 .git 同级排除
if item in (".git", ".nojekyll"):
    continue

# 层2：清理后补建
# 层3：dist 复制后最后防线
# 层4：orphan 分支自带
```

> 已提交 main 分支，commit `0687804`

---

## 🔧 三、新增采集脚本（已注册 batch_update.py close mode）

| 脚本 | 功能 | 位置 |
|------|------|------|
| `fetch_up_down_stats.py` | 涨跌家数（akshare） | 收盘后 |
| `fetch_fomc.py` | FOMC 利率数据 | 收盘后 |

---

## ⚠️ 四、小九接班注意

| 问题 | 处理 |
|------|------|
| `fetch_ipo.py` 丢失 | 需恢复或重写 |
| akshare 概念/行业资金流 API 常断 | 板块数据走模拟兜底 |
| `update_data_v2.py` 宏观采集 `save_json` bug | 会触发重跑但最终成功，暂不影响 |
| 龙虎榜/两融/北向 显示旧数据 | T+1 正常现象，次日更新 |
| .nojekyll 不会再丢 | 三层防护已部署 |

---

> **核心结论**：今晚变更集中在 UI 字体/颜色/版式统一 + .nojekyll 永久保护。明天开盘前关注 fetch_ipo.py 恢复情况即可。
