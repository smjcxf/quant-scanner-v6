# 交接文档 — 2026年6月25日

> 小九 → 阿狸咪 🤝（最后更新：6/25 18:00 · 6/26 08:00 补充）

---

## 🔥 6/25 全部修改汇总（10项）

### 1. 盘后更新标签全站补完
- 数据监控页（shmonitor）所有8张卡片时间显示均已加上 **"（盘后更新）"**
- 总览页：沪深港通资金监控卡也已加上
- `batch_update.py` 审计：移除 `fetch_herding_data.py` 从 `afternoon` 模式（盘中跑盘后数据纯浪费）

### 2. 三线共振累计涨幅全0 → 已修复
- **根因**：`generate_triple_resonance_history.py` 硬编码路径 `E:/workspace/...` 导致在本机读不到 gold_pool，`_tracking_latest` 字段始终为空
- **修复**：路径改为 `os.path.dirname(__file__)` 相对路径 + 自动从 gold_pool 历史生成 523 只股票的最新收盘价
- `update_triple_resonance_daily.py` 同样修复硬编码路径

### 3. 顶栏"三线"数字显示错误（4 vs 2）→ 已修复
- SCAN_DATA 和 WATCH_DATA 的 triple_signals 简单相加未去重，实际只有2只却显示4
- 修复：合并去重后计数

### 4. 止损/目标价逻辑升级
- **旧**：固定 -7% / +12%
- **新**：基于K线动态计算
  - 止损 = `max(当前价×0.93, 近10日最低价×0.98)`, 上限 `当前价×0.95`
  - 目标价 = `max(当前价×1.12, 近20日最高价)`
- 逻辑详解页已同步更新

### 5. 持仓建议"无信号触发"与信号灯矛盾 → 已修复
- 信号灯显示3/4（正确），但建议文字显示"无信号触发"（错误）
- **根因**：`gold_pool.latest` 所有信号为 false，`history[last]` 才是正确数据
- **修复**：数据优先级改为 `history[last] > latest`

### 6. 世界杯 6/24 比赛数据丢失 → 已修复并防复发
- 添加到 `fetch_worldcup.py` 的 `ALL_RESULTS` 硬编码列表
- 新增"保留已有 JSON 结果"兜底逻辑——API 挂了也不丢之前已获取的数据

### 7. 健康看板 MAHORO 信号恢复
- 活数据标签下恢复第二条：📡 MAHORO投行信号

### 8. 硬编码路径全量审查 ✅
- 所有 `batch_update.py` 流水线脚本均使用 `os.path.dirname(__file__)` 相对路径
- `inject_macro.py` 遗留路径也已修复
- **双机部署不会再因路径不同互相覆盖**

---

## 二、⚠️ 当前状态

| 项目 | 状态 | 说明 |
|------|------|------|
| EMA趋势 | ✅ | 在线生效，523只股票有数据 |
| 三线共振累计涨幅 | ✅ | 已修复，不再全0 |
| 世界杯数据 | ✅ | 6/11-6/24 共52场 |
| 个股主力动向 | ❌ 停更 | 6/18起，前端自动隐藏 |
| BaoStock | ❌ 黑名单 | 本机IP，mootdx兜底 |
| Eastmoney API | ❌ 被封 | 本周板块已改自算，世界杯API(thsoccerworldcups)今天也挂了 |

---

## 三、定时任务（batch_update.py 无变更）

各模式定时照旧运行：
- **pre_brief** (07:30) → 世界杯 + 全球速报
- **pre_market** (09:15) → 研报 + MAHORO + 全量扫描
- **morning_scan** (09:45) → 快速扫描
- **morning_plus** (10:00/10:30) → 扫描 + 三卡刷新
- **morning_report** (11:45) → 午间研报
- **afternoon** (13:30/14:30/16:30) → 午后扫描（已移除 fetch_herding_data）
- **close** (19:30) → 收盘全量（所有盘后数据集中跑）
- **backup** (21:00) → 备份

---

## 四、已修改文件清单（坚果云同步即可）

| 文件 | 修改内容 |
|------|----------|
| `index_master.html` | 止损/目标价动态计算、信号一致性修复、盘后更新标签、MAHORO恢复 |
| `batch_update.py` | 移除 fetch_herding_data 从 afternoon 模式 |
| `fetch_worldcup.py` | 6/24 比赛数据 + 保留已有结果兜底 |
| `generate_triple_resonance_history.py` | 路径修复 + _tracking_latest 自动生成 |
| `update_triple_resonance_daily.py` | 路径修复 |
| `inject_macro.py` | 路径修复 |
| `triple_resonance.html` | 由 update_data_v2.py 自动生成，无需手动修改 |
| `交接文档_20260625_给阿狸咪.md` | 本文件 |

---

## 五、密码

| 密码 | 权限 |
|------|------|
| `cat999` | 管理员（全部可见） |
| `hjd666` | 贵宾 |

---

## 六、接棒检查清单

- [ ] 坚果云拉取所有最新文件
- [ ] 确认网站：https://ah-quant999.github.io/quant-scanner-v6/
- [ ] 检查三线共振追踪页 `triple_resonance.html`：累计涨幅不为0
- [ ] 检查个股查询：止损/目标价不固定（不同股票不同值）
- [ ] 检查数据监控页：每张卡片均有"盘后更新"标签
- [ ] 检查健康看板活数据：有两个条目（观澜台 + MAHORO）
- [ ] 晚上 `close` 模式跑完后，世界杯 6/25 数据如果 API 还是不行，手动添加到 ALL_RESULTS
