# 九宝量化 — 2026-06-25 06:55 交接给小九

## ⚡ 最重要：新增铁律第四条

**部署前必过语法检查**。每次注入数据后、部署前，必须执行 `check_syntax.py` 对全站 HTML/JS 做语法校验（0错误才允许部署）。此检查已集成到 batch_update.py close 自动化 pipeline。

## 📋 今日凌晨改动

### 新增脚本
| 脚本 | 用途 |
|------|------|
| `check_syntax.py` | 全站 HTML/JS 语法检查，0错误 → 绿灯 |

### 集成到自动化
- `check_syntax.py` 已插入 `batch_update.py close` 模式（enhance_dist 之后、deploy_now 之前）
- 每天 19:30 收盘自动化自动执行

### 铁律更新
- 铁律从 3 条 → **4 条**，第四条：部署前必过语法检查

### 世界杯竞彩娱乐
- 07:30 早间速报自动化运行 `fetch_worldcup.py --auto`
- 晋级概率 Monte Carlo 5000次模拟
- 加权进攻/防守效率（对手强度调整）
- 各洲足联对比、小组积分榜

### 日历
- 新增 SG公假（新加坡假期）浅紫丁香色块

### 逻辑详解页更新
- 新增竞彩娱乐、健康看板、SG公假说明

## 🔧 操作指南

### 拉取代码
```bash
cd E:/workspace/stock-scanner
git pull origin main
```

### 语法检查（每次部署前必跑）
```bash
python check_syntax.py
```

### 如有语法错误
- 返回非0 → 修复后再部署
- 输出 ALL CLEAN → 可以部署

---

_生成时间: 2026-06-25 06:55_
