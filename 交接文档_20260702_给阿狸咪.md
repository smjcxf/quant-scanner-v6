# 交接文档 2026-07-02（周四）小九 → 阿狸咪

> 🚨 **铁律：代码走 GitHub，数据走坚果云**
>
> **永远记住这一句就够了**——`.py/.html/.js` 通过 `git push/pull` 同步；`data/*.json` 和交接文档通过坚果云同步。坚果云**绝对不能同步 `.git/` 和 `dist/`**。

> ⚠️ **强制第一步：三保险检查（每次交接必须执行）**
>
> 这是防止坚果云覆盖旧版代码的核心防线，**漏做 = 部署旧版 = 满意版被覆盖**。
> 已在 `sync_check.py` 集成，执行即可：
> ```bash
> cd /e/workspace/stock-scanner
> python sync_check.py
> ```
> 必须看到输出：`✅ 同步检查通过，满意版锁定正常，可以安全部署`。
> 第5步如果显示 `🚨 index_master.html 被坚果云覆盖为旧版！→ 已回退到满意版`，说明坚果云已经搞了事情，需要重新确认满意版后再部署。

---

## 一、三保险执行确认（每次交接第一步）

| 步骤 | 检查内容 | 预期结果 | 状态 |
|------|----------|----------|------|
| 1 | `python sync_check.py` 第5步 MD5 锁定 | `✓ index_master.html 满意版锁定通过` | ☐ |
| 2 | `git status` 确认无冲突 | `nothing to commit` | ☐ |
| 3 | `git log --oneline -3` 确认最新提交 | 最后一行是今天的提交 | ☐ |
| 4 | 检查 `.nutstoreignore` 存在 | `cat .nutstoreignore` 有 `.git/` 和 `dist/` | ☐ |
| 5 | 检查坚果云客户端排除设置 | `stock-scanner/.git/` 和 `dist/` 已排除同步 | ☐ |

> 如果第5步未通过，**先不部署**，检查：
> 1. 是否执行了 `python sync_check.py --lock` 锁定当前满意版
> 2. 坚果云客户端是否识别了 `.nutstoreignore`
> 3. 家里电脑是否也做了同样的 `.nutstoreignore` 设置

---

## 二、今日修改内容

### 1. 打新研判：过滤已过申购日期的股票 ✅
**问题**：打新研判显示托伦斯301583（申购日6/29已过期）。
**修复**：
- `fetch_ipo_data.py`：main()中计算评分后过滤 `apply_date < today` 的股票
- `index_master.html`：把 `continue` 移到 `for` 循环开头，过期股票不渲染任何HTML

### 2. 坚果云防覆盖三保险（根因修复） ✅
**问题**：坚果云同步 `.git/` 目录，家里电脑覆盖到公司电脑，产生58个冲突文件。
**修复**：
- `.nutstoreignore`：排除 `.git/` 和 `dist/`
- `sync_check.py`：新增第5步 MD5 锁定检查，不匹配自动 `git checkout` 回退
- `python sync_check.py --lock`：锁定当前满意版

### 3. AI市场速览：新增日经225 + 韩国KOSPI ✅
- 日经225：Sina `int_nikkei`
- 韩国KOSPI：akshare `index_global_hist_sina(symbol='首尔综合指数')`
- 自动显示在指数行，前端无需改动

---

## 三、部署说明

已推送的修改：
```
81e19b0  fix: 打新研判过滤已过申购日期的股票
25238d2  fix: 建立坚果云防覆盖三保险
e1524d5  feat: AI市场速览新增日经225+韩国KOSPI指数
```

部署命令：
```bash
cd /e/workspace/stock-scanner
git pull origin main
python sync_check.py        # 第一步！确认满意版锁定
python update_data_v2.py --fast
python deploy_now.py
```

**网址**：https://ah-quant999.github.io/quant-scanner-v6/

---

## 四、定时任务状态

| 时间 | 模式 | 状态 | 备注 |
|------|------|------|------|
| 07:30 | pre_brief | ✅ | 世界杯 |
| 09:15 | pre_market | ✅ | 盘前全量 |
| 09:45 | morning_scan | ✅ | 轻量扫描 |
| 10:30 | morning_plus | ✅ | 扫描+三卡 |
| 11:45 | morning_report | ✅ | 午间研报 |
| 13:30 | afternoon | ✅ | 午后扫描 |
| 14:30 | afternoon | ✅ | 午后扫描 |
| 15:20 | post_close | ✅ | 收盘后快速 |
| 15:30 | afternoon | ✅ | 午后扫描（新增） |
| 16:30 | afternoon | ✅ | 午后扫描 |
| 19:30 | close | ✅ | 收盘全量（并行化） |
| 21:00 | backup | ✅ | 审核备份 |

---

## 五、双机协作备忘

- 公司：小九（工作日）
- 家里：阿狸咪（周末/下班后）
- 代码同步：GitHub（`git push/pull`）
- 数据同步：坚果云（仅同步 `data/` 和交接文档，**不同步 `.git/` 和 `dist/`**）
- 满意版锁定：`.index_master_md5_lock` 文件存储 MD5，部署前自动校验

---

## 六、阿狸咪需要做的（到家后）

1. `git pull origin main` 获取今天的修改
2. 检查坚果云客户端是否排除了 `stock-scanner/.git/` 和 `stock-scanner/dist/`
3. 运行 `python sync_check.py --lock` 锁定当前满意版（如果满意版已是最新）
4. 确认 `cat .nutstoreignore` 有 `.git/` 和 `dist/`

---

祝阿狸咪晚上顺利！🐱
