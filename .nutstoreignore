# 坚果云忽略规则 — 防止同步覆盖导致版本混乱
# 核心原则：代码版本控制走 GitHub，坚果云只同步数据文件和交接文档

# 禁止同步 Git 内部文件（根因：两台电脑的 .git/ 通过坚果云互相覆盖，导致 Git 状态混乱）
.git/

# 禁止同步构建产出（dist 目录由本地构建生成，不通过坚果云传输）
dist/

# 禁止同步 Python 虚拟环境和缓存
__pycache__/
*.pyc
*.bak

# 禁止同步锁文件和临时文件
*.lock
.deploy_lock
.scanner.lock
.index_master_md5_lock

# 禁止坚果云同步代码文件（代码走 Git，坚果云只同步数据）
# 防止坚果云把 index_master.html 覆盖为旧版
index_master.html

# 允许同步的数据文件（data/ 目录需要双向同步）
# data/ 内的文件不受上述规则影响，因为 data/ 在项目根目录下
